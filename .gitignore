**/.terraform/*
bootstrap/.terraform.lock.hcl
*.tfstate
*.tfstate.*

crash.log
crash.*.log

*.tfvars
*.tfvars.json

override.tf
override.tf.json
*_override.tf
*_override.tf.json

.terraform.tfstate.lock.info

.terraformrc
terraform.rc

# Agents
settings.local.json

# IaC triage taskflow: AI_API_TOKEN, read from the repository root by
# load_dotenv(find_dotenv(usecwd=True)) when the taskflow container runs.
/.env

# Python
__pycache__/
*.pyc
